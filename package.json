{
  "name": "crud-mongo-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "yarn run build && node dist/server.js",
    "start:docker": "yarn run build:docker && docker-compose up crud-mongo-server",
    "lint": "yarn run biome check --write",
    "build": "yarn run lint && tsc && node esbuild.config.mjs",
    "build:docker": "yarn run lint && docker-compose build crud-mongo-server",
    "watch": "tsx watch src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@types/formidable": "^3.4.5",
    "@types/node": "^20.12.12",
    "esbuild": "^0.27.2",
    "lefthook": "^2.0.2",
    "tsx": "^4.19.3",
    "typescript": "~5.4.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "formidable": "^3.5.4",
    "mongodb": "^6.6.2",
    "valibot": "^1.2.0"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "packageManager": "yarn@4.3.1"
}
